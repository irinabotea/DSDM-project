<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Compose handles colors via MaterialTheme). -->
    <style name="Theme.FitPulse" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
